{
  "name": "todolist",
  "version": "1.0.0",
  "description": "todolist with parcel + pages.js + localStorage",
  "main": "main.js",
  "scripts": {
    "start": "parcel src/index.html --out-dir docs",
    "prebuild-dev": "shx rm -rf dist/* docs/*",
    "build-dev": "parcel build ./src/index.html --out-dir docs --no-minify --public-url ./",
    "prebuild": "shx rm -rf dist/* docs/*",
    "build": "parcel build src/index.html --out-dir docs --public-url ./",
    "push-gh-pages": "push-dir --dir=docs --branch=gh-pages --cleanup --verbose"
  },
  "devDependencies": {
    "shx": "^0.3.2",
    "parcel": "^1.12.4"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "parcel-bundler": "^1.12.4"
  }
}


